
function initSelect() {
    //$('select').each(function (i, obj) {
    //    var label = $(obj).data('placeholder') ?? "انتخاب کنید";
    //    var params = { searchable: $(obj).data('searchable') ?? false, placeholder: label, searchtext: 'جستجو', selectedtext: '' };
    //    NiceSelect.bind(obj, params);
    //    $(obj).removeClass('w-100');
    //});
}

